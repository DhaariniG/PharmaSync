<?php
/**
 * DEPRECATED — superseded by app/views/authentication/.
 * Auth views were moved to the shared authentication/ folder since it's
 * meant to be used by all roles, not just Customer. This file is a stale
 * duplicate left over from reorganizing the folder structure — the sandbox
 * that generated this bundle can create files but not delete them, so
 * please delete this file manually once you've confirmed
 * app/views/authentication/ has what you need.
 */
